import React, { useRef } from "react";
import {
    motion,
    useMotionTemplate,
    useMotionValue,
    useSpring,
} from "framer-motion";


export default function projectCard() {
    return (
        <div
            style={{ transformStyle: "preserve-3d" }}>

        </div>
    )
}