import { motion } from "framer-motion";

export default function SkillPage() {
    return (
        <div>
            <div>
                <motion.ul animate={{ rotate: 360 }} />
            </div>
        </div>
    )
}